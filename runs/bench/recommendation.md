# Ladder recommendation

Floors applied: C0 smoke >= 50%, schema adherence >= 95%.

**No tier clears both floors.** Do not pick a workhorse from this table: either the prompts need the budgeted iteration first (E1), or the ladder needs a larger tier.

Any tier in the roadmap ladder with no row above is a **placeholder** and must be treated as one in the thesis. A local 4-bit row is indicative of throughput and schema behaviour only; capability claims are made at bf16 on the cluster.
